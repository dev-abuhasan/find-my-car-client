import React from 'react';
// import { GetData } from '../../services/axios/https';

const Home: React.FC = () => {
    // React.useEffect(() => {
    //     const data = async () => {
    //         let getData = await GetData(`/cars/offers-cars`, false);
    //         console.log(getData);
    //     }
    //     data();
    // }, []);

    return (
        <div className='container'>
            <h1>Home Page</h1>
        </div>
    );
};

export default Home;